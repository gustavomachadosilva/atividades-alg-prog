/* Faça um programa que lê 3 strings: s1 (de tamanho máximo igual a 100), s2 (de tamanho máximo igual a 20) e s3 
(de tamanho máximo igual a 20). O programa deve gerar uma versão da string s1, substituindo todas as ocorrências de s2 por s3. 
*/

#include <stdio.h>

int main() {

    

    return 0;
}